import React from 'react'
import Navbar from '../Components/Navbar'

const Header = () => {

  return (
    <div id='navbar' className='w-full shadow backdrop-blur-sm sticky top-0 z-50'>
      <Navbar/>
    </div>
  )
}

export default Header
